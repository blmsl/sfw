{
  "functions": {
    "predeploy": [
      "npm --prefix functions-cron/functions run lint",
      "npm --prefix functions-cron/functions run build"
    ],
    "source": "functions-cron/functions"
  },
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  }
}

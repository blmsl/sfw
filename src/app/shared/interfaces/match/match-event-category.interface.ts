export interface IMatchEventCategory {
  id: number,
  title: string,
  showTextInput?: boolean,
  inputTitle?: string,
  playerOneTitle?: string,
  showPlayerOneInput?: boolean,
  playerTwoTitle?: string,
  showPlayerTwoInput?: boolean,
  parentCategory?: string
}
